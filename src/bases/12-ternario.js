
const activo = true;

// let mensaje = '';

// if ( !activo ) {
//     mensaje = 'Activo';
// } else {
//     mensaje = 'Inactivo';
// }

// const mensaje = ( activo ) ? 'Activo' : 'Inactivo';  // Operador ternario

const mensaje = activo && 'Activo';        // Si es el bool es true regresa el segundo valor
                // caso contrario regresa el false

console.log(mensaje);



